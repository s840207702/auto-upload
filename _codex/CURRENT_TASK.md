# CURRENT_TASK

## 当前任务

- 状态：已完成，等待下一步指令。
- 目标：开源前清理敏感运行数据，并补齐 README、NOTICE、SECURITY 等正规开源说明。
- 已清理：本地 `cookiesFile/`、`db/`、`videoFile/`、`logs/`、`avatars/` 目录内容已清空。
- 已清理：此前被 Git 跟踪的 `avatars/account_*.png` 真实账号头像已从工作树删除，等待下次提交时从仓库移除。
- 已清理：`_codex` 下大量调试截图/JSON 探针产物已删除，仅保留 `AGENTS.md`、`CURRENT_TASK.md`、`PROJECT_MAP.md`、`DECISIONS.md` 四个上下文文档。
- 已调整：`.gitignore` 增加 `/avatars`，并将 `_codex` 限定为只保留四个上下文文件，避免调试截图、DOM JSON、平台后台截图误入仓库。
- 已调整：`README.md` 改为“非丨自媒体一键分发”的正式开源说明，补充项目来源、参考项目、主要二开改动、当前平台发布机制优化、免责声明和安全建议。
- 已新增：`NOTICE` 记录 `zydgmail/social-auto-upload` 和 `funfan0517/MediaPublishPlatform` 的来源与参考关系。
- 已新增：`SECURITY.md` 说明敏感数据范围、开源前检查命令、误提交处理建议。
- 已新增：`conf.example.py` 作为公开示例配置，避免用户把个人配置写入 README。
- 已检查：敏感关键词扫描只命中代码字段名/本地 token 读取逻辑，未发现真实 Cookie 值。
- 上一轮：已优化预发布测试 Chrome 窗口显示，避免固定尺寸导致右侧/底部边缘看起来被截断。
- 已定位：发布浏览器窗口此前先离屏打开，再按固定 `1600x1000` 和固定坐标拉回屏幕；在不同分辨率、Windows 缩放或任务栏布局下，窗口边缘可能不像正常 Chrome 那样完整。
- 已调整：`reveal_page_window` 现在先把离屏窗口移动回主屏幕，再通过 CDP 设置 `windowState: maximized`，表现为最大化但非全屏的普通 Chrome 窗口。
- 已调整：如果 CDP 调整失败，JS 兜底改为读取 `screen.availWidth/availHeight` 并保留 24px 边距，不再强依赖固定 `1600x1000`。
- 已验证：`utils/base_social_media.py` Python 编译检查通过。
- 上一轮：已加固抖音批量定时发布时间写入，并调整发布内容模型为“公共作品文案可选、B站稿件标题单独必填”。
- 已定位：抖音定时发布时间此前只执行“点击定时 -> 输入时间 -> Enter”，没有回读校验；批量第 3 条如果页面状态慢半拍，可能漏设但流程继续。
- 已调整：抖音定时发布现在会写入 `YYYY-MM-DD HH:mm` 后回读 `.semi-input[placeholder="日期和时间"]`，最多重试 3 次；仍不一致则直接报错并保留现场，不进入后续视频。
- 已调整：发布中心公共“标题”改为“作品文案”，不再必填；抖音、视频号、小红书、快手继续把该字段作为作品描述/简介前缀，允许只填话题。
- 已调整：B站设置区新增“稿件标题”字段，B站 payload 使用该字段作为投稿标题；B站简介继续保留独立 `biliDesc`。
- 已调整：前端表单校验、后端 payload 校验只对 B站强制标题；非 B站只校验公共作品文案长度和话题数量。
- 已验证：后端 Python 编译检查通过；前端 `npm run build` 通过。
- 上一轮：已修复快手批量预发布中封面确认未收口就进入下一条视频的问题。
- 已定位：快手封面流程此前偏乐观，确认后只要检测到 toast 或弹窗短暂不可见就可能返回；批量多视频时容易表现为上一条封面裁剪/上传还没完全应用，下一条已经开始执行。
- 已调整：快手封面图片上传就绪判断排除“上传中/加载中/处理中”，确认按钮必须可见且非禁用后才点击。
- 已调整：快手点击封面“确认”后必须等待封面弹窗真正关闭，并额外稳定等待；弹窗不关闭会直接报错，不允许继续下一条视频。
- 已调整：批量 dry-run 中同一个平台某条视频失败后，不再继续执行该平台后续视频，避免第二条卡在封面弹窗时第三条继续推进；其它平台仍可按独立结果保留现场。
- 已验证：`.venv` Python 编译检查通过：`uploader/ks_uploader/main.py`、`myUtils/postVideo.py`、`main.py`。
- 当前判断：快手批量策略之前确实偏激进，现在封面弹窗关闭成为单条视频完成的硬门槛，宁可慢几秒，也不带半完成状态进入下一条。
- 上一轮：已修复小红书批量定时预发布中封面偶发失败、定时时间未设置的问题。
- 已定位：小红书新版定时控件为 `.post-time-wrapper` 内的自定义 switch 和 `.d-datepicker input.d-text`；旧 `.el-input__inner[placeholder="选择日期和时间"]` 已不适用。
- 已调整：小红书定时发布只在 `.post-time-wrapper` 作用域内打开开关、写入 `YYYY-MM-DD HH:mm`，并回读校验，失败直接报错。
- 已调整：小红书封面入口支持 `.cover-plugin-preview .default.row`、`.cover-plugin-preview .cover` 等多候选卡片，hover 后优先点击“修改封面”，失败再按卡片中心点击；封面设置失败不再静默保留默认封面。
- 已验证：小红书局部探针完成视频上传、3:4 封面上传确认、定时发布写入并回读 `2026-05-15 10:17`；未点击最终发布。
- 当前判断：调试阶段继续保留 `DEBUG_SKIP_FINAL_PUBLISH=True`，真实上传和填表，但不点击最终发表。
- 当前调试策略：预发布 dry-run 填完表单后不再按固定秒数自动关闭浏览器；窗口会保持打开，供人工检查细节，手动关闭窗口后流程再结束。
- 已调整：批量平台固定顺序为 `抖音 -> 视频号 -> B站 -> 小红书 -> 快手`，前端 `selectedPlatformAccounts` 和后端 `/postVideoBatch` 都按该顺序排序。
- 已调整：前端多平台发布不再循环调用 `/postVideo`，改为一次性调用 `/postVideoBatch`，让后端能统一创建一个浏览器窗口和多个标签页。
- 已调整：`/postVideoBatch` 在 dry-run 模式下使用 `post_video_batch_dry_run_tabs`，合并多个账号登录态到同一个浏览器 context，并按平台顺序为每个平台创建一个标签页填表。
- 已调整：5 个平台上传器支持外部传入的 `page/context/browser`，批量模式下不再各自启动/关闭独立浏览器，也不会在单个平台结束时点击最终发布或阻塞后续标签页。
- 已调整：调试阶段无论立即发布还是定时发布，`DEBUG_SKIP_FINAL_PUBLISH=True` 下只执行到最终发布前；批量标签页全部填完后统一保持浏览器打开，等待人工审查。
- 已调整：批量 dry-run 即使某个平台填表失败，也记录单个平台结果并继续保留浏览器窗口，不再直接进入 `finally` 关闭整个浏览器。
- 已调整：前端批量失败展示使用后端 `data.results` 的逐平台结果，不再把单个平台失败消息复制到所有平台。
- 已调整：快手话题改为逐个写入后立刻验收 `.at-tag-item[data-tag-name]`，缺失时只重试当前话题，提升第 5 个话题的生成稳定性。
- 已调整：快手平台配置话题上限改为 4；前端选择快手后会按当前平台最小上限截断，payload 按平台单独截断，后端入口与快手上传器也按 4 个做最终兜底。
- 已调整：抖音发布器清空/跳过独立标题 input，改为在 `.zone-container` 作品描述内写入 `标题 #话题`。
- 已调整：小红书发布器清空/跳过独立标题 input，改为在 `.tiptap.ProseMirror` 作品简介内先写入标题，再沿用现有话题生成逻辑追加话题。
- 已调整：小红书话题输入后按空格确认的等待从 `600/900/1300ms` 提升到 `1200/1800/2500ms`，`#` 后等待提升到 `300ms`，空格后验收等待提升到 `1000ms`。
- 已调整：快手封面上传不再用全页面 blob 图片判断上传完成，改为只看封面弹窗内部的 `清空上传` 或弹窗内 blob 增量，避免被视频预览/帧图误判。
- 已调整：快手封面的“上传封面”页签、比例选择、图片 input 和“确认”按钮都收紧到封面弹窗作用域，减少误点页面其它同名控件。
- 已调整：小红书话题不再使用 `insert_text` 一次性写入标签文本，改为参考快手节奏：`End -> Space -> Shift+Digit3 -> 等 800ms -> 逐字输入标签 -> 等 1800/2200/2800ms -> Space -> 等 900ms -> 回读节点`。
- 已验证：小红书用 `keyboard.type("#")` 仍可能误吸附官方推荐话题；改用 `Shift+Digit3` 后，`#1` 会先出现“新建话题”，空格确认后生成真实 `.tiptap-topic` 节点。
- 已调整：小红书话题生成失败时不再降级写普通 `#话题` 文本；必须生成 `.tiptap-topic` 节点，否则直接报错，避免界面看着有话题但实际平台不识别。
- 已验证：`.venv` Python 编译检查通过；快手 direct dry-run 已完成视频上传、描述/话题写入、`3:4` 封面上传并返回“快手封面应用成功”，最终发布被跳过。
- 已验证：小红书 direct dry-run 完成视频上传、`3:4` 封面上传、作品简介写入，并一次性回读到 `#1 #2 #3 #4` 对应 `.tiptap-topic` 节点；最终发布被跳过。
- 已调整：小红书进入发布页后先 `page.reload(wait_until="commit")`，短暂等待后再定位视频上传 input。
- 已调整：快手进入发布页后先 `page.reload(wait_until="commit")`，短暂等待后再执行 `upload_video_file`。
- 已调整：视频号定时时间输入从只写小时改为写完整 `HH:MM`，避免随机分钟被平台重置为整点。
- 已调整：B站定时发布不再只点击日期/时间控件；现在会打开定时开关、选择目标日期、选择目标小时和分钟，并回读校验。
- 已调整：B站时间选择器只支持 5 分钟粒度，随机分钟会就近落到可选 5 分钟刻度并记录日志。
- 已调整：小红书话题生成优先点击弹出的精确候选（例如 `#1 新建话题`），找不到精确候选时才退回空格确认，降低批量两条时误选官方推荐话题的概率。
- 已调整：抖音封面横/竖封面切换后增加封面编辑器就绪等待，避免 UI 切换未完成就上传下一张封面。
- 已调整：批量 dry-run 生成定时时间时会打印最终排程、基准时间、开始天数和随机浮动，便于核对各平台拿到的目标时间。
- 约束：优先读取 `_codex/AGENTS.md`、`_codex/CURRENT_TASK.md`、`_codex/PROJECT_MAP.md`、`_codex/DECISIONS.md`。
